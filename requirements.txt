PySide6>=6.6.0
opencv-python>=4.8.0
scikit-image>=0.22.0
scipy>=1.11.0
numpy>=1.24.0
openpyxl>=3.1.0
xlsxwriter>=3.2.0
python-pptx>=1.0.0
qtawesome>=1.3.0
Pillow>=10.0.0
# tifffile: already an scikit-image dependency; used by the test-suite to
# write synthetic vendor-tagged SEM TIFFs (runtime reader is stdlib-only).
tifffile>=2023.1.0
# CPU-only wheels keep the installer small; build scripts/CI install them with
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
torchvision>=0.15.0
segment-anything>=1.0
